För python indentering:

	set tabstop=4       " The width of a TAB is set to 4.
			    " Still it is a \t. It is just that
			    " Vim will interpret it to be having
			    " a width of 4.

	set shiftwidth=4    " Indents will have a width of 4

	set softtabstop=4   " Sets the number of columns for a TAB

	set expandtab       " Expand TABs to spaces

För att göra jj till escape och ändra timeouten
	:inoremap jj <Esc>
	:set timeoutlen=50set tabstop=4

Use repeat operator with a visual selection:
	vnoremap . :normal .<cr>

Plugins:
	Pathogen
	Gundo
	syntastic
	nerdtree
	powerline
	jedi
	Fugitive
	git-gutter

Install: pathogen
    mkdir -p ~/.vim/autoload ~/.vim/bundle
    curl -LSso ~/.vim/autoload/pathogen.vim https://tpo.pe/pathogen.vim

add "execute pathogen#infect()" to .vimrc
later install all plugins under ~./.vim/bundle

nerdtree: https://github.com/scrooloose/nerdtree.git
gundo: https://github.com/sjl/gundo.vim.git
syntastic: https://github.com/scrooloose/syntastic.git
powerline: https://github.com/powerline/powerline.git
jedi: https://github.com/davidhalter/jedi-vim.git
